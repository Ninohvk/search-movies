import React from "react";

export const Title = ({ children }) => <h1 className="title">{children}</h1>;

// Recibiremos un children, es decir lo que va entre las etiquetas, el contenido.
// className en lugar de class ya que en js es reservada.
